name: pgpx

on:
  workflow_dispatch:
  push:
    tags: [ "v[1234].[0-9]+.[0-9]+" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.20'

    - name: Retrieve branch information
      id: branch_info
      run: |
        echo ::set-output name=SOURCE_NAME::${GITHUB_REF#refs/*/}
        echo ::set-output name=SOURCE_BRANCH::${GITHUB_REF#refs/heads/}
        echo ::set-output name=SOURCE_TAG::${GITHUB_REF#refs/tags/}

    - name: Build for darwin/amd64 (mac intel)
      run: |
        GOOS=darwin GOARCH=amd64 go build -o pgpx-mac-intel pgpx.go

    - name: Build for darwin/arm64 (mac Mx)
      run: |
        GOOS=darwin GOARCH=arm64 go build -o pgpx-mac-m pgpx.go

    - name: Upload artifact (mac intel)
      uses: actions/upload-artifact@v3
      with:
        name: pgpx-mac-intel-${{ steps.branch_info.outputs.SOURCE_TAG }}
        path: ./pgpx-mac-intel

    - name: Upload artifact (mac Mx)
      uses: actions/upload-artifact@v3
      with:
        name: pgpx-mac-m-${{ steps.branch_info.outputs.SOURCE_TAG }}
        path: ./pgpx-mac-m
